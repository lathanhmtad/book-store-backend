server:
  port: 8080

spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: true
  datasource:
    url: jdbc:mysql://localhost:3306/book_store
    username: root
    password: 1234

# app properties
com:
  book:
    jwtCookieName: jwt
    jwtSecret: ======================jwt=spring===========================
    jwtExpirationMs: 86400000 # 1 hour