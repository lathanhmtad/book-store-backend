server:
  port: 8080

spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: true
  datasource:
    url: jdbc:mysql://localhost:3306/book_store?createDatabaseIfNotExist=true
    username: root
    password: 1234

# app properties
com:
  book:
    cloudinary:
      imageFolder: book-store-images
    jwt:
      jwtSecret: ======================jwt=spring===========================
      jwtExpirationMs: 3600000
